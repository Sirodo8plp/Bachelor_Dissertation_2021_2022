query findPostcardByID($id: String!) {
  findPostcardById(id: $id) {
    id
    description
    location {
      city
      region
    }
    photographs {
      id
      imageLink
      transactionHash
    }
  }
}
